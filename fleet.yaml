defaults:
  command: claude
  args: ["--dangerously-skip-permissions"]
  ready_pattern: "bypass permissions"
  cols: 120
  rows: 40

channel:
  type: telegram
  bot_token_env: AGEND_BOT_TOKEN
  group_id: -1003725098111
  mode: topic

instances:
  general:
    role: "Rust terminal test — general coordinator"
    working_directory: /tmp/agend-terminal-test-claude
    topic_id: 1
