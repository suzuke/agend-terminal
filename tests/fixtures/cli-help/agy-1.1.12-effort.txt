# Provenance: captured 2026-09-06 on Darwin via `agy --help` (agy 1.1.12)
# Purpose: pins the effort-flag grammar EffortCapability depends on (issue #3541, L2 fixture)
# Minimal excerpt: only the effort-flag-relevant help lines are retained.
# Note: `--effort` takes a space-separated value like `--model` on the adjacent
# help line (`--model   Model for the current CLI session`); accepted values are
# declared inline as (low|medium|high).
#
  --effort                        Reasoning effort for the current CLI session (low|medium|high)
