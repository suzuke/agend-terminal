[package]
name = "agentic-audit-append"
description = "Serialized append to the fleet audit log: pre-serialize the whole line, then hold a companion advisory lock for exactly one write_all"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
fs4 = "1.1"
serde_json = "1"
