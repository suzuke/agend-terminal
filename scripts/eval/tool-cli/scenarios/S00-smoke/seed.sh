#!/usr/bin/env bash
# S00-smoke seeds nothing: it exists so the harness itself can be exercised
# end to end without spending a graded pair.  Args: HOME PAIR (both ignored).
set -euo pipefail
echo '{}'
