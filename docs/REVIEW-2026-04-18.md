# Codebase Review 2026-04-18

**Scope**: full src/ (~22k LOC, 40 modules) + tests + docs.
**Method**: 8 parallel Explore sub-agents, one per subsystem. Top P0/P1 claims re-verified by direct file read before inclusion.

## Summary

~120 raw findings collapsed to ~40 actionable items across 4 tiers.
Strengths: test isolation, MCP/daemon unit coverage, rustls consistency, backend enum unification (partial).
Weaknesses: disk durability (no fsync/atomic rename in several paths), authorization (Telegram/MCP/IPC have no authn), god-modules (5 files >1100 LOC), schema versioning absent.

Two sub-agent claims were **withdrawn after verification**:
- `framing.rs:48` OOM pre-allocation — FALSE. Length bound check precedes `vec!`. Confirmed at lines 48–53.
- `mcp/mod.rs:42` Content-Length=0 "infinite loop" — REFRAMED. Not a loop; it's protocol desync when malformed header skipped without consuming its body.

---

## P0 — Data integrity / protocol correctness (fix first)

| # | Location | Issue |
|---|---|---|
| P0-1 | `event_log.rs:32-33` | Rotation only keeps `.jsonl.1`; second rotation overwrites it. History past ~20MB lost. |
| P0-2 | `event_log.rs:37-52` | No `sync_all()`; kernel buffers lose events on crash. |
| P0-3 | `snapshot.rs:23-33` | `fs::write()` direct, no temp+rename+fsync. Crash mid-write → corrupt JSON → fleet state loss on restart. |
| P0-4 | `inbox.rs:78-86` | Drain: rename → read. If read fails, `.draining` orphan + messages lost. No recovery on startup. |
| P0-5 | `mcp_config.rs:84,212,243,296` | Claude/Kiro/Gemini/OpenCode backend configs written via direct `fs::write`. Only Codex (`:380`) uses flock. Concurrent spawn corrupts config. |
| P0-6 | `decisions.rs:78-84` | `post()` handling `supersedes` is load→modify→save; two callers race and clobber each other. |
| P0-7 | `fleet.rs:273` | Lock file opened with `truncate(true)` — lock state destroyed on open. Defeats mutual exclusion under racing openers. |
| P0-8 | `mcp/mod.rs:78-83` | JSON-RPC parse error `continue`s silently. Spec requires `{"error": {"code": -32700, ...}, "id": null}`. Client hangs. |
| P0-9 | `decisions.rs::post()` | Id `d-YYYYMMDDHHMMSS` collides when two callers post within the same second; second write clobbers the first's JSON file. Surfaced by Stage 2 supersede test. Absorbed in Stage 2 (id now includes nanoseconds + per-process AtomicU64 sequence). |
| P0-10 | `mcp/mod.rs:42` | `Content-Length` header with value `0` or unparseable digits desyncs the stream (empty separator not consumed; next frame shifted into body). Fold of Stage 1 follow-up. Absorbed in Stage 2. |

## P1 — Security / Authorization

| # | Location | Issue |
|---|---|---|
| P1-1 | `telegram.rs:199-269` | No user allowlist. Any group member controls fleet. |
| P1-2 | `mcp/mod.rs:102-115` | No per-tool / per-caller authz. Any stdio-reachable process invokes all tools. |
| P1-3 | `ipc.rs` + `api.rs:62-76` | TCP 127.0.0.1 with no UID check and no read/write timeout. Same-host user attack + slow-client thread starvation. |
| P1-4 | `api.rs:309` | `working_directory` param has no path bounds. `../` escapes. |
| P1-5 | `bugreport.rs:179-200` | Redaction token list incomplete (missing `password`/`secret`/`api_key`/`bearer`/`authorization`). |
| P1-6 | `agent.rs:203-205` | `config.env` HashMap applied raw → fleet.yaml can override `ANTHROPIC_API_KEY` etc. Fixed in Stage 4 (sensitive-env deny-list on spawn). |
| P1-7 | `deployments.rs:75-97` | YAML `name_val.as_str()` passes unvalidated into git worktree path. Fixed in Stage 4 (validate_name on template / deploy-name / instance suffix / combined). |
| P1-8 | `agent.rs:222` | `create_dir_all(working_dir).ok()` tolerates symlink redirection. Fixed in Stage 4 (double-pass `validate_working_directory` pre- and post-create). |
| P1-9 | `instructions.rs:111-148` | `ctx.name`/`ctx.role` interpolated into markdown without escaping. Backticks in instance name → prompt injection. Fixed in Stage 4 (identifier/role sanitisers). |
| P1-10 | `api.rs:62-76` / `ipc.rs` | TCP loopback cannot cheaply authenticate peer UID. Stage 3 shipped timeouts only; UDS migration (plus Windows named-pipe) still owed. |

## P2 — Concurrency / integrity

| # | Location | Issue |
|---|---|---|
| P2-1 | `daemon.rs` 6 mutex sites | All mutex poison paths were swallowed via `into_inner()`. Fixed in Stage 5 (`sync::lock_poisoned` helper logs every recovery). |
| P2-2 | `daemon.rs:246` crash channel | Unbounded `crossbeam::channel` starved shutdown under crash flood. Fixed in Stage 5 (bounded(64) + `try_send` with warn-log on full). |
| P2-3 | `daemon.rs:209-221` | Verified not-a-real-TOCTOU in Stage 5: the flock is held by `find_active_run_dir()` before the port-file read; two daemons cannot both acquire the exclusive lock. Documented for future reference. |
| P2-4 | `agent.rs:378-386` | 500ms delay before reading instructions file — external mutation window. Fixed in Stage 5 (bootstrap now takes a pre-read `content: String`; file is read at spawn time). |
| P2-5 | `fleet.rs:287-297` | `mutate_fleet_yaml` already holds `acquire_file_lock` for the full read→mutate→write cycle after Stage 2. Verified in Stage 5 — not a real bug. |
| P2-6 | `schedules.rs:12` | `Box::leak(TZ)` per env read → unbounded heap. Fixed in Stage 5 (`OnceLock<String>` caches detection for process lifetime). |
| P2-7 | `schedules.rs:70,87` | Cron evaluated in naive local tz; DST transition shifts job by 1h. Fixed in Stage 5 (`chrono-tz` aware evaluation per-schedule). |
| P2-8 | All store files | No `version: u32`; schema drift silently deserializes with defaults. Fixed in Stage 5 (`SchemaVersioned` trait + `load_versioned` / `mutate_versioned` helpers; schedules/tasks/teams/deployments stamped `schema_version: 1`). |

## P2 — Architecture

- God modules: `layout.rs`(1403), `render.rs`(1277), `daemon.rs`(1249), `fleet.rs`(1169), `mcp/handlers.rs`(1149). `daemon.rs` hits PID-reuse / lifecycle / TUI / Telegram / cron / CI watch / respawn / health.
- Backend enum unified but only Codex got atomic config writes. Others still direct-write.
- `app/overlay.rs:144,195` — `mem::replace(overlay, None)` loses state if subsequent pane creation fails.
- PTY numeric risks: `vterm.rs:100` scroll_offset can push Line index negative (fixed in Stage 6a: `scroll_offset.min(i32::MAX as usize) as i32` clamp + `saturating_sub` on grid_line); `vterm.rs:153` i32→u16 silent truncation (`cursor_pos` is `#[allow(dead_code)]` — left as-is with note); `render.rs:660/683/751` u16 addition can overflow bounds checks (fixed in Stage 6a: `saturating_add`/`saturating_sub` throughout tab-title rendering and `render_menu`).

## P2 — Tests / CI

- `api.rs` 762 LOC, **1 test**.
- No tests: `ipc.rs` integration, `schedules` cron/tz, trust gates, deployments template expansion, per-backend end-to-end spawn.
- `.github/workflows/ci.yml:40` runs `cargo test --test integration` on Windows; `tests/integration.rs` hardcodes `/bin/bash` → red on Windows matrix.
- Positive: `AGEND_HOME` isolation clean; MCP/daemon unit coverage solid.

## P3 — Docs

Healthy. PLAN-*.md marked DONE where complete. `MCP-TOOLS.md` notes `command` param deprecated — verify CHANGELOG has sunset.

---

## Fix stage plan

| Stage | Scope | Status |
|---|---|---|
| **1** | P0-1, P0-2, P0-3, P0-8 — disk durability & JSON-RPC error response | done |
| **2** | P0-5 (other backend atomic writes), P0-4 (inbox drain recovery), P0-6 (decisions atomic), P0-7 (fleet lock), plus absorbed P0-9 (decisions id collision) and P0-10 (MCP Content-Length desync) | done |
| **3** | P1-1 through P1-5 — authz & secret hygiene (partial on P1-3: timeouts done, peer-UID gate deferred with note) | done |
| **4** | P1-6 through P1-9 — input validation on dangerous surfaces | done |
| **5** | P2 concurrency: daemon.rs poison strategy, schedules DST/leak, store versioning | done |
| **6a** | P2 architecture (narrow): render/vterm numeric safety + remaining daemon.rs poison sites + spawn-without-home warn | done |
| **6b** | P2 architecture (wide): `daemon.rs` (1249 LOC) split into per-concern modules + fleet-wide `sync::lock_poisoned` sweep | todo |
| **7** | Tests/CI: api.rs coverage, Windows CI matrix fix, trust gate tests | todo |

Each stage: worktree → changes + tests → local merge to main → update this doc → handover.

---

## Progress log

### Stage 1 — 2026-04-18 — done
Branch: `worktree-review-fix-stage1`
Items:
- [x] P0-1 event_log rotation (multi-generation)
- [x] P0-2 event_log fsync
- [x] P0-3 snapshot atomic write
- [x] P0-8 MCP JSON-RPC parse-error response

**Files touched**
- `src/snapshot.rs` — added `save_atomic()`; temp file → fsync → rename. `save()` now warns on error.
- `src/event_log.rs` — multi-generation rotation (MAX_GENERATIONS=5, oldest pruned); `sync_all()` after each append; added in-file tests.
- `src/mcp/mod.rs` — parse errors now return JSON-RPC `-32700` response with `id` salvaged from raw body when possible.
- `tests/mcp_roundtrip.rs` — `test_parse_error_returns_response`, `test_parse_error_salvages_id_when_possible`.

**Tests added**: 5 (2 snapshot, 2 event_log including rotation + prune, 2 MCP parse-error). All pre-existing tests still pass.

**Verification**: `cargo test` green (integration 6/6, mcp_roundtrip 12/12, lib 332/332). `cargo clippy --all-targets` clean.

**Known follow-ups surfaced by this stage** (not absorbed; queued elsewhere):
- `mcp/mod.rs:42` Content-Length parse returning 0 still desyncs the stream rather than emitting a protocol error. Different surface from P0-8. Add to Stage 2 scope.
- `snapshot.rs` load path has no checksum — a power-loss window that corrupts the renamed target (extremely rare with fsync-then-rename but possible on pathological FS) will still yield `None` silently. Low priority, noted.
- `event_log::log()` has no caller-visible error channel; daemon cannot react if the audit trail becomes unwritable (disk full). Architectural, file under Stage 5.

### Stage 2 — 2026-04-18 — done
Branch: `worktree-review-fix-stage2`
Items:
- [x] P0-4 inbox drain recovery (`.draining` file left intact on read failure; leftover recovered on next drain)
- [x] P0-5 backend configs atomic write + flock (claude / kiro / gemini / opencode; codex already had flock but was re-routed through shared helper)
- [x] P0-6 decisions `post()` supersedes under per-decision flock; `update()` also holds flock for read→modify→write
- [x] P0-7 fleet lock file no longer opened with `truncate(true)`; switched to shared `acquire_file_lock` helper
- [x] P0-9 decisions id collision for same-second posts (absorbed — format now `d-{YYYYMMDDHHMMSS + 6 nanos}-{AtomicU64 seq}`)
- [x] P0-10 MCP `Content-Length` header with `0` / malformed value now always consumes the separator line and does not desync; parse errors log a warn and resync

**Files touched**
- `src/store.rs` — new shared helpers `atomic_write()`, `save_atomic()`, `acquire_file_lock()` (no `truncate(true)`, with fsync on data files). `mutate()` refactored on top of them.
- `src/mcp/mod.rs` — Content-Length framing: parse failure → warn + consume separator + continue; zero length → consume separator + continue (no body read into next frame).
- `src/inbox.rs` — `drain()` split into rename step and `read_drain_file()`; leftover `.draining` from a prior crash is surfaced first; `.draining` only removed after successful parse so a read failure can be retried.
- `src/decisions.rs` — per-decision `with_decision_lock()`; `save()` / `post()` supersedes / `update()` now all acquire the per-id flock for the entire read→modify→write cycle; id generation uses nanoseconds + process-wide AtomicU64 sequence.
- `src/fleet.rs` — `acquire_lock()` and `atomic_write_yaml()` delegate to the shared `store` helpers.
- `src/mcp_config.rs` — per-config-path flock + atomic write for claude statusline + settings, kiro wrapper + mcp.json (single lock covers legacy-key cleanup + upsert), gemini settings, opencode settings, codex trust directory.
- `tests/mcp_roundtrip.rs` — `test_content_length_zero_skips_frame_without_desync`, `test_invalid_content_length_resyncs_to_next_frame`.

**Tests added**: 12 (5 `store` helpers, 3 `inbox` drain-recovery cases, 2 `decisions` supersede + concurrent-update, 2 MCP framing). All pre-existing tests still pass.

**Verification**: `cargo test` green — bin unit 340/340, `integration` 6/6, `mcp_roundtrip` 14/14. `cargo clippy --all-targets` clean.

**Known follow-ups surfaced by this stage** (not absorbed; queued elsewhere):
- Inbox drain still drops a malformed line inside a batch (JSON parse failure per-line logs a warn but discards that one entry). Acceptable — matches prior behavior — but worth a future "quarantine" path. Low priority, file under Stage 5.
- `mcp_config.rs` now holds a flock per config path; if a caller crashes after creating the lock file but before writing content, an empty `.lock` file lingers. Harmless (locks are inode-based) but clutters the directory. Housekeeping only.
- `decisions.rs` id is now per-process unique only. Two daemons (contrary to single-instance invariant) could still collide. Depends on Stage 2 P2-3 TOCTOU fix; noted for Stage 5.

### Stage 3 — 2026-04-18 — done
Branch: `worktree-review-fix-stage3`
Items:
- [x] P1-1 Telegram user allowlist plumbed through `ChannelConfig::Telegram.user_allowlist` → `TelegramState.user_allowlist` → enforced in `handle_message`. Legacy `None` = open mode (startup warn); `Some([])` = reject all; `Some([...])` = strict.
- [x] P1-2 MCP per-tool ACL via `AGEND_MCP_TOOLS_ALLOW` / `AGEND_MCP_TOOLS_DENY` env vars (comma-separated). Deny overrides allow. Blocked tools are hidden from `tools/list` AND rejected from `tools/call` with JSON-RPC `-32601`.
- [x] P1-3 Read/write timeouts (30s) applied to every accepted API session. Peer-UID gate requires UDS migration — see new P1-10.
- [x] P1-4 `validate_working_directory()` added: rejects `..` components, canonicalises the deepest existing ancestor, verifies path stays under `AGEND_HOME` or `AGEND_ALLOWED_WORK_ROOTS` (colon-separated). Wired at the SPAWN handler.
- [x] P1-5 `bugreport::redact_secrets()` expanded token list (`password`, `secret`, `api_key`, `bearer`, `authorization`, `credential`, etc.) and switched to case-insensitive match on the key portion.

**Files touched**
- `src/bugreport.rs` — expanded redaction token list, case-insensitive match on key side only (value text no longer triggers false positives), added unit tests.
- `src/api.rs` — new `validate_working_directory()` helper + wiring at SPAWN; 30s read/write timeouts on accepted sessions; `env_guard()` test mutex for env-mutating tests.
- `src/mcp/mod.rs` — `tool_acl()` / `tool_is_allowed()` / `filter_tools()` helpers; `tools/list` filters, `tools/call` rejects blocked tools before proxy.
- `src/fleet.rs` — `ChannelConfig::Telegram` gained `user_allowlist: Option<Vec<i64>>` (serde default None); existing tests updated to match-through with `..`.
- `src/telegram.rs` — `TelegramState` gained `user_allowlist`; `is_user_allowed()` helper; `handle_message` drops non-allowlisted senders with warn log; startup logs deprecation warn when `None`.

**Tests added**: 16 (4 bugreport, 4 api path-validation, 5 MCP ACL, 3 telegram allowlist). All pre-existing tests still pass.

**Verification**: `cargo test` green — bin unit 356/356, `integration` 6/6, `mcp_roundtrip` 14/14. `cargo clippy --all-targets` clean.

**Known follow-ups surfaced by this stage** (filed as new items, not silently absorbed):
- **P1-10** (new) — Peer-UID check on the daemon API still requires migrating loopback TCP to a Unix Domain Socket (plus a named-pipe equivalent on Windows). Timeouts reduce the slow-client blast radius, but same-host users on the same UID can still read `api.port` and speak the protocol. UDS migration is a non-trivial cross-platform change; queue under Stage 5 or later as its own work unit.
- Telegram allowlist defaults to **open mode** for backward compatibility with existing deployments. A follow-up patch should flip the default to `Some([])` with a migration note, OR prompt at `init` for the first-party Telegram user ID. Log lines already surface the deprecation. Not urgent; noted.

### Stage 4 — 2026-04-19 — done
Branch: `worktree-review-fix-stage4`
Items:
- [x] P1-6 `agent.rs` env deny-list. `SENSITIVE_ENV_KEYS` covers Anthropic/OpenAI/Gemini API keys, AWS credentials, GitHub/GitLab/NPM tokens, dynamic-linker injection vectors (`LD_PRELOAD`, `DYLD_INSERT_LIBRARIES`, etc.), and agend's own runtime wiring (`AGEND_HOME`, `AGEND_MCP_TOOLS_*`). Applied at the `config.env` loop; blocked keys are warn-logged and dropped. Matching is case-insensitive for cross-platform safety (Windows env is case-insensitive).
- [x] P1-7 `deployments.rs` name validation. Template name, deploy name, instance suffix, and combined instance name all go through `agent::validate_name`. Template is checked first so a missing `name` (which defaults to `template`) still produces a clear template-name error. Invalid suffixes are skipped with a warn, leaving good instances deployable.
- [x] P1-8 `agent.rs:222` working-dir symlink audit. Replaced raw `create_dir_all(dir).ok()` with a two-pass `api::validate_working_directory` (pre-create for the path-has-no-`..` invariant, post-create so canonicalisation walks through any symlink and the starts_with check catches escape). When `home` is absent (ad-hoc test spawns) the recheck is skipped to preserve existing test ergonomics.
- [x] P1-9 `instructions.rs` markdown escaping. Added `sanitize_identifier` (restricts to `[A-Za-z0-9_-]`, 64 char cap) for backtick-span fields and `sanitize_role_text` (strips backticks and control chars, 256 char cap) for the free-form role text. Both applied to `ctx.name`, `ctx.role`, peer names, and peer role descriptions.

**Files touched**
- `src/agent.rs` — new `SENSITIVE_ENV_KEYS` + `is_sensitive_env_key()`; `spawn_agent` drops deny-listed env and double-validates the working directory around `create_dir_all`.
- `src/deployments.rs` — `deploy()` validates template / deploy_name / instance suffix / combined inst_name; test module added.
- `src/instructions.rs` — `sanitize_identifier()` / `sanitize_role_text()` helpers applied in `build_instructions_body`; injection tests added.
- `docs/REVIEW-2026-04-18.md` — this stage log, status flip, annotated P1 rows.

**Tests added**: 12 (3 `agent::is_sensitive_env_key`, 3 `deployments::deploy` input validation, 6 `instructions` sanitiser + injection). All pre-existing tests still pass.

**Verification**: `cargo test` green — bin unit 368/368 (was 356 before the stage), `integration` 6/6, `mcp_roundtrip` 14/14. `cargo clippy --all-targets -- -D warnings` clean.

**Known follow-ups surfaced by this stage** (filed, not silently absorbed):
- `deployments::deploy` still issues backend commands by string (`"claude"`, `"codex"`, etc.) without verifying the backend is among the known set. Not a security issue per se — `api::SPAWN` will reject unknowns — but produces noisier logs. Housekeeping, not urgent.
- `agent.rs` symlink recheck depends on `home` being threaded through `SpawnConfig`. Daemon paths already do; tests intentionally pass `None`. If a future code path spawns from production with `home: None`, the recheck silently degrades. Add a debug-assert in Stage 5 or Stage 6 when `daemon.rs` gets refactored.
- P1-9 sanitizer deletes rather than escapes. If a future instructions template needs to reproduce user-supplied role text verbatim (e.g. for debugging), consider switching to a backtick-escape strategy instead of a strip.

### Stage 5 — 2026-04-19 — done
Branch: `worktree-review-fix-stage5`
Items:
- [x] P2-1 mutex poison paths. Added `src/sync.rs::lock_poisoned(mutex, label)` that preserves the existing recovery semantics (the crash reaper thread depends on these locks to do its job, so bailing the daemon on poison would itself be worse) but emits a structured `tracing::error!(lock = label)` on every recovery. Migrated the six review-flagged sites in `daemon.rs` (configs ~264, periodic-snapshot ~425, session-id poll ~467, respawn config lookup, health record_crash ~503, saved_health ~550, respawn-success restore + inject ~607/~620). The non-flagged poison sites (`cfgs` on shutdown, `registry` drain, `child` kill) kept the inline recovery to bound the diff; a future sweep can adopt the helper.
- [x] P2-2 bounded crash channel. `daemon::run` changed to `crossbeam::channel::bounded::<String>(64)`; `agent::handle_pty_close` now uses `try_send` and warn-logs on full. 64 comfortably exceeds a plausible fleet-wide simultaneous crash and `try_send` means a stalled reaper never wedges the PTY close handler — one dropped event at most skips auto-respawn, which the health probe will catch on the next tick.
- [x] P2-3 startup-lock TOCTOU. Verified by reading `find_active_run_dir` + `acquire_file_lock`: the flock is exclusive and held by the first daemon for its lifetime; a second daemon that races through the directory discovery blocks on `lock_exclusive()` before reading the port file, so the documented scenario cannot occur. Flagged as verified-not-a-real-bug in the table.
- [x] P2-4 instructions bootstrap race. `spawn_instructions_bootstrap` now takes a pre-read `content: String` instead of a path. Call site reads the file at spawn time; empty/missing/unreadable skips bootstrap. The 500ms external-mutation window is closed.
- [x] P2-5 `mutate_fleet_yaml` check-then-act. Verified already resolved by Stage 2 — `fleet::mutate_fleet_yaml` acquires `store::acquire_file_lock` before `serde_yaml::from_str` and holds it across the `atomic_write_yaml` save. Flagged as verified-not-a-real-bug in the table.
- [x] P2-6 TZ leak. `schedules::detect_timezone()` switched from `Box::leak` per call to a module-level `OnceLock<String>`. Detection still honours `TZ` env → `/etc/localtime` symlink → `"UTC"` fallback, but the first successful detection is cached for process lifetime; subsequent `$TZ` toggles are intentionally ignored (schedules carry their own per-row `timezone` field which remains the source of truth for cron evaluation).
- [x] P2-7 cron DST. Added `chrono-tz = "0.10"` dep. `daemon::check_schedules` now parses each schedule's `timezone` field into `chrono_tz::Tz` (fallback `UTC` on unknown), converts `now_utc` and `last_check_utc` with `with_timezone(&tz)`, and evaluates `schedule.after(&last_check_local)` so DST transitions no longer shift jobs by 1h.
- [x] P2-8 store versioning. New `store::SchemaVersioned` trait + `load_versioned` / `mutate_versioned` helpers; `ScheduleStore`, `TaskStore`, `TeamStore`, `DeploymentStore` each carry `#[serde(default)] schema_version: u32` and `const CURRENT: u32 = 1`. Older files (no field) load as version 0 and upgrade on next save. Files whose `schema_version > CURRENT` refuse to load with an `error!` log, returning default rather than silently downgrading. `decisions.rs` is intentionally out of scope — it is a per-record file layout rather than a Store wrapper, so versioning doesn't map cleanly and carries no silent-downgrade risk beyond the per-record schema.

**Files touched**
- `Cargo.toml` — added `chrono-tz = "0.10"`.
- `src/sync.rs` — new module: `lock_poisoned<T>(mutex, label)` helper + 2 tests.
- `src/main.rs` — register `mod sync;`.
- `src/daemon.rs` — 6 poison sites converted to `sync::lock_poisoned`; crash channel to `bounded(64)`; `check_schedules` rewritten for tz-aware cron evaluation (`now_utc`/`last_check_utc`, per-schedule `chrono_tz::Tz`).
- `src/agent.rs` — `handle_pty_close` uses `try_send` on bounded channel; `spawn_instructions_bootstrap` takes `content: String` instead of path, read at call site.
- `src/schedules.rs` — `detect_timezone()` via `OnceLock<String>`, now `pub`; `ScheduleStore` versioned; `mutate_versioned` in place of `mutate`.
- `src/tasks.rs` / `src/teams.rs` / `src/deployments.rs` — same versioning pattern (field + trait impl + `load_versioned` / `mutate_versioned` or versioned save).
- `src/store.rs` — `SchemaVersioned` trait + `load_versioned` + `mutate_versioned` helpers; unused generic `mutate()` removed now that all in-tree callers are on the versioned variant; 3 tests added (`load_versioned_accepts_equal_or_older_version`, `load_versioned_refuses_future_version`, `mutate_versioned_stamps_current_on_save`).
- `docs/REVIEW-2026-04-18.md` — this stage log, status flip, annotated P2 rows.

**Tests added**: 5 (2 `sync::lock_poisoned`, 3 `store` versioning). Total bin unit: **373** (was 368). `integration` 6/6, `mcp_roundtrip` 14/14.

**Verification**: `cargo test` green — 373/373 unit, 6/6 integration, 14/14 mcp_roundtrip. `cargo clippy --all-targets -- -D warnings` clean. Build with `--all-targets` clean.

**Known follow-ups surfaced by this stage** (filed, not silently absorbed):
- `daemon.rs` still holds a handful of non-flagged `.unwrap_or_else(|e| e.into_inner())` call sites on shutdown and child-kill paths. They don't meet the "hot path during steady-state" bar of the review, but a Stage 6 sweep that adopts `sync::lock_poisoned` everywhere would make the logging coverage uniform.
- `crossbeam::channel::bounded::<String>(64)` was chosen as a round number bigger than any realistic fleet. If operators ever run fleets larger than ~50 agents *and* hit simultaneous crashes (e.g. host oom-killer), the bound may need bumping or the policy reworked to drop oldest instead of newest. Not urgent.
- `store::load_versioned` peeks the JSON once then calls `load` which re-reads and re-parses. On the pathological path this doubles I/O; acceptable for the call frequency (startup + mutate) but a Stage 6+ refactor could share the parsed `Value`.
- `P2-8` schema-version guard only catches *JSON-level* layout divergence. A change that keeps the shape but changes field semantics (e.g. a units switch) still deserializes silently. The review recommendation was "version or refuse"; we chose refuse-on-future and accepted-on-equal-or-older, which matches typical forward-compat conventions.
- The Stage 4 note about `agent.rs:home: None` silently skipping the symlink recheck is still outstanding — appropriate target is Stage 6 when `daemon.rs` gets split and the `SpawnConfig::home` always-threaded invariant can be enforced at the type level.

### Stage 6a — 2026-04-19 — done
Branch: `worktree-review-fix-stage6`
Scope: the low-risk half of what was originally one "Stage 6". The big `daemon.rs` architectural split is carved out as Stage 6b so the correctness fixes land independently of a refactor.

Items:
- [x] **Numeric safety — vterm.rs.** `render_to_buffer` and `extract_text` both clamped `scroll_offset` with `.min(i32::MAX as usize) as i32` (was a bare `as i32` that wraps negative on 64-bit hosts for huge values), and replaced `row as i32 - offset` with `(row as i32).saturating_sub(offset)`. A pathological scroll offset now degrades to "deepest scrollback" instead of underflow-panicking the alacritty grid index.
- [x] **Numeric safety — render.rs.** Tab-title rendering (`render_pane_titles` loop at ~660-685) now uses `saturating_add`/`saturating_sub` for `last_usable_x`, `buf_right`, `buf_bottom`, `x + w` bound check, wide-char trail indices, and the final `x += w` step. `render_menu` (~750) switched `items.len() as u16` to `u16::try_from(..).unwrap_or(u16::MAX)` and clamped the `area.height - 2` / `area.width - 4` computations with `saturating_sub`. Before: u16 wraparound could bypass a bound check and let `buf[(x, y)]` index outside the Rect. After: clipped rendering on pathological sizes instead of silent OOB.
- [x] **Stage 5 follow-up — remaining `daemon.rs` poison sites.** Converted the 5 non-flagged `.unwrap_or_else(|e| e.into_inner())` call sites to `crate::sync::lock_poisoned(&mutex, "label")`: two TUI-server sites (`pty_writer` / `pty_master` at ~111/121), the shutdown `configs` read (~663), the registry drain (~692), and the per-child kill (~700). Logging coverage on poison recovery is now uniform across `daemon.rs`.
- [x] **Stage 4 follow-up — `SpawnConfig::home` degradation signal.** Replaced the silent else-branch in `agent::spawn_agent`'s working-dir handling with a `tracing::warn!(instance, dir, "spawn without AGEND_HOME — working_directory symlink recheck skipped")`. Tests continue to spawn with `home: None` (tracing silent under `cargo test`), but any future production path that regresses and drops home now surfaces loudly in live daemon logs rather than quietly losing the second-pass symlink check.

**Files touched**
- `src/vterm.rs` — clamp + saturating_sub in `render_to_buffer` and `extract_text`.
- `src/render.rs` — saturating arithmetic in tab-title rendering loop and `render_menu`.
- `src/daemon.rs` — 5 more poison sites migrated to `sync::lock_poisoned`.
- `src/agent.rs` — warn-log on `home: None` working-dir path.
- `docs/REVIEW-2026-04-18.md` — this stage log, Stage 6 split into 6a (done) / 6b (todo), P2-architecture numeric-risks row annotated.

**Tests added**: 0 — every change is a defensive refactor of an existing hot path (numeric safety clamp, poison-recovery logging, warn-log on a previously-silent degradation). Existing test suite (373 unit + 6 integration + 14 mcp_roundtrip = 393 total) covers every touched function and all remain green.

**Verification**: `cargo test` green — 373/373 unit, 6/6 integration, 14/14 mcp_roundtrip. `cargo clippy --all-targets -- -D warnings` clean. `cargo build --all-targets` clean.

**Known follow-ups surfaced by this stage** (filed, not silently absorbed):
- `vterm::cursor_pos` still does `i32 as u16` on `c.line.0` / `c.column.0` at line ~161. Marked `#[allow(dead_code)]` and has no callers; left as-is rather than grow the diff. If a future change reaches for it, apply the same clamp (`.max(0) as u16`) at that call site.
- Codebase-wide `.unwrap_or_else(|e| e.into_inner())` pattern still exists in `cli.rs`, `api.rs`, `agent.rs`, `verify.rs`, `mcp/mod.rs`, `app/mod.rs` (~40 sites). Not a *bug* — the recovery semantics are correct — but uniform logging via `sync::lock_poisoned` is the Stage 6b sweep target.
- The numeric-safety pattern is defensive against a pathological caller; no test was added because constructing such a caller (Rect with `u16::MAX` coordinates, scroll_offset > i32::MAX) requires either unsafe or harness-specific fixtures that would be more fragile than the saturating arithmetic they'd be proving.

---

## Handover

**Next stage to pick up: Stage 6b.**

Stage 6b scope: **`daemon.rs` split + fleet-wide poison-sweep.**

- `daemon.rs` (1249 LOC after Stage 6a) still touches PID-reuse / lifecycle / TUI server / Telegram dispatch / cron / CI watch / respawn / health in one file. Suggested split along lifecycle axes — module per concern:
  - `daemon/respawn.rs` — crash channel reception + spawn_agent respawn closure + health restoration
  - `daemon/cron.rs` — `check_schedules` + tz-aware evaluation (this stage landed the tz fix; the function is self-contained and is the easiest extract)
  - `daemon/telegram_dispatch.rs` — the Telegram notification send path
  - `daemon/tui_bridge.rs` — the `serve_agent_tui` and related IPC threads (the two poison sites at ~111/121 plus their handlers)
  - `daemon/lifecycle.rs` or keep in `daemon.rs` — the top-level `run()` glue
- Keep `run()` itself small and readable: compose the per-concern modules; everyone takes the shared `registry` / `configs` / `shutdown` by `Arc`.
- While splitting, do a fleet-wide sweep: every `.unwrap_or_else(|e| e.into_inner())` in `src/` migrates to `crate::sync::lock_poisoned(&mutex, "<stable_label>")`. Stable labels make the logs correlatable (e.g. `"api_registry"`, `"cli_registry"`, `"verify_core"`); pick one that names the module and lock. Sites to sweep (as of Stage 6a): `cli.rs` (~15), `api.rs` (3), `agent.rs` (~13), `verify.rs` (~15), `mcp/mod.rs` (1), `app/mod.rs` (1).
- `layout.rs` (1403 LOC) and `render.rs` (1277 LOC) also qualify for splits but were marked lower-risk in the original review — treat them as Stage 6c if they turn out to be in the way.

**Separately queued (not Stage 6b):**
- **P1-10** — peer-UID gate via UDS / named-pipe migration. Cross-platform; its own dedicated stage, probably between Stage 6b and Stage 7 or deferred until Windows support lands.

Suggested reading order for whoever picks up Stage 6b:
- `src/daemon.rs` top-to-bottom — note the concerns by eye before deciding on module boundaries. Stage 6a changes are small and localized; the structure is unchanged since Stage 5.
- `src/sync.rs` — trivial; 1 helper + 2 tests. The helper signature `lock_poisoned(&Mutex<T>, &'static str)` works transparently for `Arc<Mutex<T>>` too since Arc derefs.
- Grep for `.unwrap_or_else(|e| e.into_inner())` in `src/**` — pattern is 100% mechanical; the judgment call is picking a stable lock label per site.

**For any session continuing this review/fix workflow:**

Read this file top to bottom. The "Fix stage plan" table is the source of truth — pick the next `todo` stage and:

1. Open a new worktree: `EnterWorktree name=review-fix-stageN` (or `git worktree add`).
2. Implement the stage's items. Keep scope to the listed items — do not expand.
3. Add/adjust tests for every touched path. User rule: automate verification by default.
4. Run `cargo test` and `cargo clippy` clean before merging.
5. Local merge to main (no PR — user rule: pure local changes skip PR).
6. Update this doc: check off items, flip stage status to done, append a new `### Stage N — date — done` entry under "Progress log" summarizing: files touched, tests added, known follow-ups.
7. If the stage reveals a new finding, append it to the appropriate tier table with a new ID; do not silently absorb it.
8. Leave a fresh handover at the bottom pointing to the next stage.

**Rules that bound this work** (from user MEMORY):
- Fix now, never defer. If a related bug is found mid-stage, fix it in the same stage — do not TODO it.
- English-only comments.
- No install/build-release without explicit consent.
- No manual binary copy to `/opt/homebrew/bin`.
- Verify numbers/claims before asserting. grep/wc yourself.
- Local merge, no PR, for pure local / no-CI changes.
